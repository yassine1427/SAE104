<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">

        <link rel="stylesheet" href="https://bootswatch.com/5/united/bootstrap.min.css">
        <link rel="stylesheet" href="/path/to/your/custom/style.css">
        {% block stylesheets %}
            <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
            <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
            <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css">
            <style>
                body {
                    background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
                    min-height: 100vh;
                }

                .card {
                    background-color: rgba(255, 255, 255, 0.9);
                    backdrop-filter: blur(10px);
                    border: none;
                    border-radius: 15px;
                    box-shadow: 0 8px 20px rgba(0,0,0,0.1);
                    transition: transform 0.3s ease, box-shadow 0.3s ease;
                }

                .card:hover {
                    transform: translateY(-5px);
                    box-shadow: 0 12px 25px rgba(0,0,0,0.15);
                }

                .card-header {
                    background: linear-gradient(135deg, #007bff 0%, #0056b3 100%);
                    border-radius: 15px 15px 0 0 !important;
                    padding: 1rem;
                }

                .btn-primary {
                    background: linear-gradient(135deg, #007bff 0%, #0056b3 100%);
                    border: none;
                    border-radius: 25px;
                    padding: 0.5rem 1.5rem;
                    transition: transform 0.2s ease, box-shadow 0.2s ease;
                }

                .btn-primary:hover {
                    transform: translateY(-2px);
                    box-shadow: 0 5px 15px rgba(0,123,255,0.4);
                }

                .timeline-item {
                    position: relative;
                    padding-left: 20px;
                    margin-bottom: 20px;
                    border-left: 2px solid #007bff;
                }

                .list-unstyled li {
                    transition: transform 0.2s ease;
                }

                .list-unstyled li:hover {
                    transform: translateX(10px);
                }

                .img-profile {
                    transition: transform 0.3s ease;
                }

                .img-profile:hover {
                    transform: scale(1.05);
                }

                .contact-info p {
                    transition: transform 0.2s ease;
                    margin-bottom: 0.5rem;
                }

                .contact-info p:hover {
                    transform: translateX(5px);
                }

                /* Style pour les sections */
                .section-title {
                    position: relative;
                    padding-bottom: 10px;
                    margin-bottom: 20px;
                }

                .section-title::after {
                    content: "";
                    position: absolute;
                    bottom: 0;
                    left: 0;
                    width: 50px;
                    height: 3px;
                    background: linear-gradient(135deg, #007bff 0%, #0056b3 100%);
                    transition: width 0.3s ease;
                }

                .section-title:hover::after {
                    width: 100px;
                }

                /* Animation unique pour les éléments */
                .fade-in {
                    opacity: 0;
                    animation: fadeIn 0.5s ease forwards;
                }

                .slide-in-left {
                    opacity: 0;
                    animation: slideInLeft 0.5s ease forwards;
                }

                .slide-in-right {
                    opacity: 0;
                    animation: slideInRight 0.5s ease forwards;
                }

                @keyframes fadeIn {
                    from {
                        opacity: 0;
                        transform: translateY(20px);
                    }
                    to {
                        opacity: 1;
                        transform: translateY(0);
                    }
                }

                @keyframes slideInLeft {
                    from {
                        opacity: 0;
                        transform: translateX(-30px);
                    }
                    to {
                        opacity: 1;
                        transform: translateX(0);
                    }
                }

                @keyframes slideInRight {
                    from {
                        opacity: 0;
                        transform: translateX(30px);
                    }
                    to {
                        opacity: 1;
                        transform: translateX(0);
                    }
                }

                /* Style pour les compétences */
                .skill-item {
                    position: relative;
                    padding-left: 25px;
                    margin-bottom: 10px;
                }

                .skill-item::before {
                    content: "▹";
                    position: absolute;
                    left: 0;
                    color: #007bff;
                }
            </style>
        {% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
            <script>
                document.addEventListener('DOMContentLoaded', function() {
                    // Ajouter les délais d'animation
                    document.querySelectorAll('[data-animation-delay]').forEach(element => {
                        const delay = element.getAttribute('data-animation-delay');
                        element.style.animationDelay = delay + 's';
                    });
                });
            </script>
        {% endblock %}
    </head>
    <body class="bg-white">
    <nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
        <div class="container-fluid">
            <meta charset="UTF-8"> <meta name="viewport" content="width=device-width, initial-scale=1.0"> <title>UJM Logo</title> <style> .logo { font-family: 'Arial', sans-serif; font-size: 48px; font-weight: bold; color:rgb(0, 0, 0); /* Blue color */ } </style> </head> <body> <div class="logo">UJM </div> </body>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarColor01">
                <ul class="navbar-nav mx-auto">
                    <li class="nav-item">
                        <a class="nav-link active" href="{{ path('app_home') }}">Home
                            <span class="visually-hidden">(current)</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="{{ path('app_loisirs') }}" class="btn btn-primary">My hobies</a>
                    </li>
                    <li class="nav-item">
                        <a href="{{ path('app_cv') }}" class="btn btn-primary">My cv</a>
                    </li>
                    <li class="nav-item">
                        <a href="{{ path('app_eportfolio') }}" class="btn btn-primary">My eportfolio</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
    
    {% block body %}{% endblock %}
    
    <footer class="text-center mt-5">
        <p>&copy; 2025 Yassine Zakri. All rights reserved.</p>
    </footer>
    </body>
</html>